# L3 — GSQ-for-MQ3V2 (charter)

| Field | Value |
|---|---|
| State | **slice 1 LS win; slice 3 ls-w tie; slice 2 Gumbel falsified (MSE tied LS) — L3 encoder done** |
| Date | 2026-08-30 |
| Sources | [GSQ](https://arxiv.org/abs/2604.18556), [RCO](https://arxiv.org/abs/2605.00649), Hy4 encoder lesson (weighted LS vs amax) |
| Floor | published `qwen3.8-27b.mq3-pro` sha256 `394c5096…`, local WT2 KLD **0.130304**, PPL **6.775** |
| Product to beat | `qwen3.8-27b.mq4-pro` local WT2 KLD **0.032715** (not a size-class peer) |
| Eval | same teacher as MIX: `~/.hipfire/kldref/qwen3.8-27b.ref_wt2.native-llama-b10488.bin` md5 `3c0268e9…`, `eval_hipfire --kv-mode q8 --kv-v q8 --scoring-mode prefill --max-chunks 24` |

N6 already applied Hy4 **allocation** on mq3 (`ssm_out` MQ6, lm_head Q8). The remaining gap is the **3-bit encoder**. Published mq3-pro used minmax per 128-weight half: `step = (hi-lo)/7`, `zero = lo`, round-to-nearest. Post-FWHT halves are near-Gaussian; minmax spends the grid on outliers. That is the Hy4 `d = amax` failure at 3 bits.

Slice 1 (2026-08-30): `quantize_mq3g256v2` default is LS + reassign on the same qt=49 wire (`HIPFIRE_MQ3V2_FIT=minmax` for the published encoder). Unit tests: pack/unpack, degenerate match, Gaussian MSE never-regress and strict win. Candidate: `~/.hipfire/models/qwen38-27b.mq3v2.pro.ls.hfq`.

## Non-goals

- Do not change the qt=49 wire (104 B/group, per-128 fp16 scale+zero, 3-bit payload). Kernels stay.
- Do not leave the mq3 bpw class (`ladder.md` honesty invariant: model bpw ∈ [3, 4)).
- Do not port IQ2 / STQ / GGUF K-quant.
- Do not unmask serve. Do not cite Hub WT2 0.130 against this teacher.
- Do not start L1 stitcher / L2 ΔKLD DP / L6 AIME until a candidate is close on this teacher **or** slice 1–2 are falsified.
- Do not residual-promote onto V2-xt (that is mq4-pro).

## Slices (one by one)

1. **LS + reassign (this slice).** Minmax init, then Lloyd: assign `q ∈ {0..7}`, least-squares `(s,z)` given `q`, fp16 round-trip, reassign. Same 104 B blob. Unit test: reconstruction MSE ≤ minmax on Gaussian halves (never-regress). Then encode a 27B `mq3-pro` candidate and 24-chunk WT2 vs published mq3-pro.
2. **Gumbel-Softmax (falsified 2026-08-30).** Deterministic Concrete (Gumbel noise = 0) on the same qt=49 wire: minmax init, 16-step τ anneal, soft `q` → LS `(s,z)`, fp16 snap, hard assign. Never-regress vs unweighted LS. Opt-in `HIPFIRE_MQ3V2_FIT=gumbel`. Proto MSE **tied** LS on Gaussian (268.64) and heavy-tail (2194.14) — softmax annealing never beat hard Lloyd on this 8-level affine grid. **No 27B encode.** L3 encoder work stops here.
3. **Imatrix-weighted LS** if slice 1 wins reconstruction but not KLD. Reuse existing GGUF imatrix join; do not invent a new sensitivity.
4. **L1/L2** only if we need mixed qt49/qt44/Q8 *inside* the mq3 class. Not another 16.46G knapsack.

## Pass / fail

- Slice 1 unit tests pass (wire + MSE never-regress).
- 24/24 chunks finite, no KLD=0.
- Promote toward product only if local WT2 KLD drops enough vs **0.1303** to justify a new mq3-pro encode. Beating mq4-pro 0.0327 is not required to ship a better mq3 SKU; it is required to *replace* mq4-pro.

## Slice 1 result (2026-08-30, same llama-b10488 teacher)

Candidate `~/.hipfire/models/qwen38-27b.mq3v2.pro.ls.hfq` — same recipe as published mq3-pro (`mq3v2` / `--tier pro` / `--fixed-tier ssm_out:mq6v2` / imatrix + AWQ α=0.55). Census F16 753 / MQ3V2 448 / MQ6V2 48 / Q8 50. Bytes **13184433152** (same as published). Metadata `hipfire_mq3v2_fit=ls`. First MQ3 tensor payload differs from minmax. Encode ~13 min CPU, binary md5 `a62984c8…`.

| variant | WT2 KLD | PPL | p99 KLD |
|---|---:|---:|---:|
| mq3-pro (minmax) | 0.130304 | 6.775 | 2.383 |
| **mq3-pro-ls** | **0.109235** | **6.610** | 2.535 |
| mq4-pro | 0.032715 | 6.331 | 1.053 |

Astrea vs mq3-pro: **`quality_improved`** (KLD −0.0211, recovered 21.6% of the gap to mq4-pro). vs mq4-pro: **`no_quality_gain`**. Mean improved; p99 slightly worse. Do not Atlas. Do not unmask serve. Do not replace published mq3-pro yet.

### Slice 3 result (imatrix-weighted LS)

Candidate `~/.hipfire/models/qwen38-27b.mq3v2.pro.ls-w.hfq`, metadata `hipfire_mq3v2_fit=ls-w`, same 13 184 433 152 B / census. Column imatrix applied as a post-FWHT diagonal proxy.

| variant | WT2 KLD | PPL |
|---|---:|---:|
| mq3-pro (minmax) | 0.130304 | 6.775 |
| mq3-pro-ls | 0.109235 | 6.610 |
| **mq3-pro-ls-w** | **0.108887** | **6.599** |

Weighted LS is a **tie** vs unweighted LS (−0.00035 KLD, inside noise). Slice 3 is falsified as a further lever. Do not spend another encode on this weighting.

### Slice 2 result (Gumbel-Softmax, 2026-08-30)

Opt-in `HIPFIRE_MQ3V2_FIT=gumbel` on the same qt=49 wire. Unit tests: Gaussian and heavy-tail reconstruction MSE **tied** unweighted LS (delta 0; never-regress holds). Hard Lloyd on 8 affine levels already sits at the MSE floor this softmax anneal can reach. **No 27B encode.** L3 encoder work is done. Do not start L1/L2/L6. Product remains mq4-pro. Optional later: refresh published mq3-pro to unweighted LS (`HIPFIRE_MQ3V2_FIT=ls`) without overwriting Hub-pinned `qwen3.8-27b.mq3-pro` sha256 `394c5096…`.

## Disk

Keep published mq3-pro. New encode lands next to it (`…mq3v2.pro.ls.hfq` or similar). ~13 GB. Do not delete scored artifacts unless asked.
