# 2026-08-24 — Qwen3.8-27B MQ4 DFlash 2 vs MTP vs AR on merge_sort (R9700)

**Lifecycle: `historical`.** Evidence under the exact fixture and method below.
Not a current default, not an automatic baseline, not an admission decision.

## Why this run exists

The default-prompt row
[`2026-08-24-qwen38-27b-dflash2-vs-mtp-ar-r9700.md`](2026-08-24-qwen38-27b-dflash2-vs-mtp-ar-r9700.md)
showed DFlash 2 accepting (τ=2.43) without a tok/s win. Serve smoke on
`merge_sort` had τ=6.88 / second-turn 93.5 tok/s under a different harness.
This row is the **same protocol-lite** as that prose cell, on the serve code
prompt bytes.

## Fixture

Same host / binaries / trunk / draft / MTP sidecar as the prose row
(hipfire `d8a4677d…`, daemon `51ef5fe2…`, trunk md5 `129909ad…`, draft md5
`cdf5dd28…`). Same harness: `--runs 3 --warmups 3 --max-tokens 128 --backend
noslots --workload stateless --kv-mode q8 -j`, `nix develop`,
`HIPFIRE_DPM_WARMUP_SECS=10`, GPU lock, fresh process per arm.

| | |
|---|---|
| prompt | `Write a Python function named merge_sort that sorts a list of integers in ascending order. Return only the function, no explanation.` |
| prompt md5 | `51a6c7360e00a7853521a6575975b8e3` |
| MTP | `--spec mtp` **and** `HIPFIRE_QWEN_MTP=1` |
| DFlash 2 | `--spec dflash` and `HIPFIRE_DFLASH_DRAFT` pinned |

## Result

| arm | decode | prefill | wall | TTFT | τ |
|---|---:|---:|---:|---:|---:|
| AR | 36.6 | 444.6 | 35.7 | 83.2 ms | — |
| MTP | 75.7 | 353.3 | 71.3 | 104.0 ms | 3.74 |
| **DFlash 2** | **84.7** | 320.8 | 78.6 | 115.3 ms | **6.94** |

JSON: [`…-ar.json`](2026-08-24-qwen38-27b-dflash2-code-r9700-ar.json),
[`…-mtp.json`](2026-08-24-qwen38-27b-dflash2-code-r9700-mtp.json),
[`…-dflash.json`](2026-08-24-qwen38-27b-dflash2-code-r9700-dflash.json).

Samples: AR decode 36.6×3. MTP 75.7 / 75.8 / 75.7, τ 3.74, `mtp_windows=34`.
DFlash 2 84.7 / 84.8 / 84.5, τ 6.94, 16 windows. Log: DFlash 2 draft detected,
not DFlash 1.

Vs AR: MTP **+107%**, DFlash 2 **+131%**. Serve second-turn 93.5 tok/s was
max=64 on this prompt; this 128-token first-turn protocol-lite sits in the
same band, not the prose 37 tok/s band.

## Reading

Genre-conditional, as the plan allowed. Prose: MTP wins. Code: DFlash 2 wins
and converts τ into tok/s. Same binaries. Not pager. Not Whittle. Not a
product default.
