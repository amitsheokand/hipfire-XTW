---
title: 32k MQ4 MTP think-low serve battery on R9700
date: 2026-08-25
tags: [r9700, gfx1201, mq4, mtp, thinking, serve]
---

Outstanding writer probe after the 32k think-off MTP battery: `--thinking low`
at `max_seq=32768`. `--max-tokens 512` is **invalid** with budget low (512) —
harness hard-fails think-only. Used **768** so the 512 think cap can still
emit an answer (same as the 2048 think-on smoke).

**Fixture:** Qwen3.8-27B MQ4 md5 `129909ad`, MTP on, greedy, kv q8, DFlash off,
prompts md5 `f2a9b231` (factual + merge_sort). Bins `d66acfe8` / `01486177`,
commit `a92d0bec`. Port 11526. `HIPFIRE_SERVE_ALLOW_INCOHERENT=1` only to
record the known code-turn n-gram false positive.

| turn | gen | finish | τ | mtp_windows | ar_windows | decode | attractor |
|---|---:|---|---:|---:|---:|---:|---|
| factual | 396 | stop | 2.88 | 137 | 0 | 57.0 | 0 |
| code | 606 | stop | 3.20 | 189 | 0 | 63.1 | 1 (false +) |

No OOM. Language OK (Paris/Seine/Eiffel + complete `merge_sort`). Token
counts match the 2048 think×MTP cell — greedy + short prompts, 32k is KV
**capacity** not filled context. JSON `/tmp/r9700-think-low-32k/battery.json`.
