---
title: Whittle v2.1 MQ4 encode and AR smoke on R9700
date: 2026-08-24
tags: [gfx1201, r9700, whittle, moe, mq4, k192]
---

`logic65/Qwen3.8-Whittle-MoE-27B-A17.8B` v2.1 streamed LoRA merge (`scripts/whittle_merge_lora.py`, scale=2.0, 304 LoRA + 88 `modules_to_save`) → `~/.hipfire/hf-cache/Qwen3.8-Whittle-MoE-27B-A17.8B/v2.1-merged/`. v2 BF16 shards deleted after merge verify (disk); adapter kept.

Encode: `hipfire-quantize --format mq4 --uniform --no-q8-router --threads 10` (arch_id 6). Artifact `~/.hipfire/models/qwen38-whittle-27b-a17.8.mq4` 16 GB, md5 `bb8dd08b909b3e5249db1d3f9e477953`. Routed `down` K=192 is Q8F16 (not HFQ4G128: 192%128≠0). gate_up / attn / shared / router MQ4; embed Q8; conv1d Q8.

AR smoke (thinking off, spec off, kv q8, max=64): Paris/Seine + merge_sort. attractor=0. `finish=length` is the cap. Landmark text is "Louie Tower" on both CPU and GPU paths (model, not a kernel bug).

- CPU-top-K fallback (before k=16 GPU): ~26 tok/s. Needed `HIPFIRE_GRAPH_MOE=0` / `HIPFIRE_AR_GRAPH=0` (D2H under capture → hipError 906). Fallback GEMV of gate_up must use prerotated `x_rot_local` when `x_rot_prerotated`.
- k=16 GPU indexed decode (2026-08-24, daemon `ed41b109d6136a8e33d0670372d7c145`): `use_gpu_topk` for k∈{8,16} + MQ4 gate_up / Q8 down. `moe_topk_renorm_k16`, gfx1201 wave64 router gated to k=8, silu without FWHT, Q8 down `n_ranks` + all-ones scale, self-combine. Graphs capture-safe. Serve: avg decode **39.0 tok/s** graphs off / **40.3 tok/s** graphs on. Same two prompts, attractor=0. JSON `/tmp/qwen38-whittle-k16.json` and `/tmp/qwen38-whittle-k16-graph.json`. Prefill stayed per-token (~40 tok/s).
- Batched k=16 + MQ4/Q8 prefill (2026-08-24, daemon `2f54c9fb112ebc73e0cb01f46390495e`): `HIPFIRE_DEBUG_BATCH=1` `all_layers_ok=true`. Path 1 (no grouped Q8 GEMM). gfx12 MQ6-admit arm must list Q8 down or the live path stays per-token. Warm serve (same prompts, graphs on): prefill **135 / 140 tok/s** (t1/t2), decode **40.2 / 40.5**, attractor=0. JSON `/tmp/qwen38-whittle-k16-prefill-warm.json`. Cold first batched prefill was 36.7 tok/s (JIT). Still ~3× behind dense MQ4 prefill (~399 tok/s protocol-lite). Not a bench claim.

Not a bytes/token or admission claim. Next: matched `hipfire bench` vs dense MQ4 if quoting tok/s; then pager traces / q* only if bytes/token says so. Decode vs dense (~36.6 protocol-lite) is still the 16×192 cost.
