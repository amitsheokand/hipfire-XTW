---
title: Whittle v2.1 MQ4 encode and AR smoke on R9700
date: 2026-08-24
tags: [gfx1201, r9700, whittle, moe, mq4, k192]
---

`logic65/Qwen3.8-Whittle-MoE-27B-A17.8B` v2.1 streamed LoRA merge (`scripts/whittle_merge_lora.py`, scale=2.0, 304 LoRA + 88 `modules_to_save`) → `~/.hipfire/hf-cache/Qwen3.8-Whittle-MoE-27B-A17.8B/v2.1-merged/`. v2 BF16 shards deleted after merge verify (disk); adapter kept.

Encode: `hipfire-quantize --format mq4 --uniform --no-q8-router --threads 10` (arch_id 6). Artifact `~/.hipfire/models/qwen38-whittle-27b-a17.8.mq4` 16 GB, md5 `bb8dd08b909b3e5249db1d3f9e477953`. Routed `down` K=192 is Q8F16 (not HFQ4G128: 192%128≠0). gate_up / attn / shared / router MQ4; embed Q8; conv1d Q8.

AR smoke (thinking off, spec off, kv q8, max=64): Paris/Seine + merge_sort. attractor=0. `finish=length` is the cap. Landmark text is "Louie Tower" on both CPU and GPU paths (model, not a kernel bug).

- CPU-top-K fallback (before k=16 GPU): ~26 tok/s. Needed `HIPFIRE_GRAPH_MOE=0` / `HIPFIRE_AR_GRAPH=0` (D2H under capture → hipError 906). Fallback GEMV of gate_up must use prerotated `x_rot_local` when `x_rot_prerotated`.
- k=16 GPU indexed decode (2026-08-24, daemon `ed41b109d6136a8e33d0670372d7c145`): `use_gpu_topk` for k∈{8,16} + MQ4 gate_up / Q8 down. `moe_topk_renorm_k16`, gfx1201 wave64 router gated to k=8, silu without FWHT, Q8 down `n_ranks` + all-ones scale, self-combine. Graphs capture-safe. Serve: avg decode **39.0 tok/s** graphs off / **40.3 tok/s** graphs on. Same two prompts, attractor=0. JSON `/tmp/qwen38-whittle-k16.json` and `/tmp/qwen38-whittle-k16-graph.json`. Prefill stayed per-token (~40 tok/s).
- Batched k=16 + MQ4/Q8 prefill (2026-08-24, daemon `2f54c9fb112ebc73e0cb01f46390495e`): `HIPFIRE_DEBUG_BATCH=1` `all_layers_ok=true`. Path 1 (no grouped Q8 GEMM). gfx12 MQ6-admit arm must list Q8 down or the live path stays per-token. Warm serve (same prompts, graphs on): prefill **135 / 140 tok/s** (t1/t2), decode **40.2 / 40.5**, attractor=0. JSON `/tmp/qwen38-whittle-k16-prefill-warm.json`. Cold first batched prefill was 36.7 tok/s (JIT). Still ~3× behind dense MQ4 prefill (~399 tok/s protocol-lite). Not a bench claim.

Not a bytes/token or admission claim. Matched protocol-lite `hipfire bench` vs dense MQ4 is recorded in `docs/perf-checkpoints/2026-08-24-whittle-vs-dense-mq4-r9700.md` (commit `506681c4` bins). Decode **40.1 vs 36.6 tok/s (+9.6%)**; prefill **132 vs 401 (−67%)**. 16×192 ate most of the FFN sparsity win.

llama.cpp R9700 thread ([discussion #21043 comment 18038995](https://github.com/ggml-org/llama.cpp/discussions/21043#discussioncomment-18038995), JohnTDI-cpu 2026-08-16): dense **Qwen3.8-27B Q8_0 + MTP** on Vulkan/RADV, `parallel=3`, `n-max=7`, KV Q4_0, mixed 30-request server load → **~65 tok/s** (53–79). Same thread's **AR** peer is opening-post Qwen3.5-27B Q4_K_M tg128 **~29–33 tok/s**. Do not treat 65 as an AR target. Our MQ4 AR 36.6 already sits above that Q4 AR; our MQ4 MTP protocol-lite is 47.8 τ=2.40 (K=3, noslots). Their levers that are not ours: ubatch=288 (llama.cpp graph), three 16k slots, Q8 weights, Q4 KV, desktop-connected GPU. Shared: leave DPM on auto; a larger AMD DMMV workgroup was slower for them and was reverted. Comment does not mention Whittle.

Profile (`HIPFIRE_PROFILE_DECODE=1`, `bench_qwen35_mq4`, graphs off): Q8 indexed down was the MoE leftover — 72 µs, **220 GiB/s** vs gate_up 435 and QKVZA 455. QKVZA is still the largest GPU slice (~27%). Lever: `gemv_q8_0_moe_down_k16_staged_r16` (stage 16×192 once, 256-thread CTA, not a denser DMMV). Timer after: 46 µs, **343 GiB/s**. Protocol-lite decode **43.3 vs 40.1 (+8.0%)**, prefill unchanged 132.8. Daemon `0e7581d1d0aecdf39e5b2ec4839a64d5`. Serve greedy smoke attractor=0; landmark still "Louie Tower". Record: `docs/perf-checkpoints/2026-08-24-whittle-q8-staged-r16-r9700.md`. Opt out `HIPFIRE_WHITTLE_Q8_STAGED=0`.

Prefill Path 1 leftover was the same kernel with `batch>1` skipping the stage (`HIPFIRE_PROFILE=1 --prefill 128`: 63% GPU, 248 GiB/s). Lifted `grid.z=N`. Protocol-lite prefill **203.9 vs 132.8 (+54%)**, TTFT **118 vs 181 ms**, decode held 43.4. Daemon `07159020cef5352b46da2d358a2b2deb`. Serve prefill ~221 tok/s, attractor=0. Record: `docs/perf-checkpoints/2026-08-24-whittle-q8-staged-prefill-r9700.md`. Dense MQ4 prefill still ~401.

Shared-expert MQ4 down was the next Path 1 slice: fused sigmoid GEMV ~20% GPU vs residual WMMA 17 ms. n>1 now memset+`GemmHfq4G256Residual`+sigmoid-add (Q8 shared-down pattern). Protocol-lite prefill **241.4 vs 203.9 (+18%)**, TTFT **99 vs 118 ms**, decode held 43.5. Daemon `ca0089115ef118956f46f03c4dc53d1c`. Serve prefill ~266 tok/s, attractor=0. Record: `docs/perf-checkpoints/2026-08-24-whittle-shared-down-wmma-r9700.md`. Leftover with headroom: routed Q8 down (~42% GPU, 776 GiB/s). Gate_up indexed already ~1100 GiB/s. Pager / q* still not indicated.
